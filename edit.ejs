<!DOCTYPE html>
<html lang="en">
    <style>
        body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
      text-align: center;
    }
    
    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    
    .title {
      text-align: center;
      margin-bottom: 20px;
    }
    
    .add-link,
    .edit-link,
    .delete-link {
      display: inline-block;
      margin-right: 10px;
      color: #0000cc;
      text-decoration: none;
    }
    
    .no-news {
      text-align: center;
      font-style: italic;
      color: #888888;
    }
    
    .news-list {
      list-style: none;
      padding: 0;
    }
    
    .news-item {
      border: 1px solid #cccccc;
      border-radius: 5px;
      padding: 10px;
      margin-bottom: 10px;
      background-color: #ffffff;
    }
    
    .news-title {
      margin: 0;
    }
    
    .news-type,
    .news-description,
    .news-instructions {
      margin-top: 5px;
      margin-bottom: 10px;
    }
    
    .form {
      border: 1px solid #cccccc;
      border-radius: 5px;
      padding: 20px;
      background-color: #ffffff;
    }
    
    .form-label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }
    
    .form-input,
    .form-textarea {
      width: 100%;
      padding: 5px;
      margin-bottom: 10px;
      border: 1px solid #cccccc;
      border-radius: 3px;
    }
    
    .form-button {
      display: block;
      margin-top: 10px;
      padding: 8px 16px;
      background-color: #0000cc;
      color: #ffffff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    
    .search-form {
      text-align: center;
      margin-bottom: 20px;
    }
    
    .search-input {
      padding: 5px;
      border: 1px solid #cccccc;
      border-radius: 3px;
      margin-right: 5px;
    }
    
    .search-button {
      padding: 5px 10px;
      background-color: #0000cc;
      color: #ffffff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    
    </style>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit News</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <div class="container">
    <h1 class="title">Edit News</h1>

    <form method="POST" action="/edit/<%= news.id %>" class="form">
      <label for="title" class="form-label">Title</label>
      <input type="text" id="title" name="title" value="<%= news.title %>" required class="form-input"><br>

      <label for="type" class="form-label">Type</label>
      <input type="text" id="type" name="type" value="<%= news.type %>" required class="form-input"><br>

      <label for="description" class="form-label">Description</label>
      <textarea id="description" name="description" required class="form-textarea"><%= news.description %></textarea><br>

   
      <button type="submit" class="form-button">Update</button>
    </form>
  </div>
</body>

</html>
